<!DOCTYPE html>
<html lang="ru">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Группа Альфа — IT-автоматизация бизнеса: 1С, кассы, ЕГАИС, маркировка, Меркурий</title>
<meta name="description" content="Подключаем к ЕГАИС, Честному знаку, Меркурию, ФГИС ЛК, Зерно. Внедряем 1С, кассы по 54-ФЗ. Готовим к цифровому рублю. Сахалин и вся РФ с 2015 года.">
<meta name="keywords" content="автоматизация бизнеса, автоматизация торговли, 1С франчайзи, онлайн-касса, ЕГАИС, Честный знак, Меркурий, ФГИС ЛК, ФГИС Зерно, цифровой рубль, ЭДО, маркировка товаров, 54-ФЗ">
<meta name="robots" content="index, follow, max-image-preview:large">
<meta name="yandex-verification" content="">
<meta name="google-site-verification" content="">
<link rel="canonical" href="https://gruppa-alfa.ru/">

<!-- Favicons -->
<link rel="icon" type="image/svg+xml" href="/favicon.svg">
<link rel="icon" type="image/x-icon" href="/favicon.ico">
<link rel="apple-touch-icon" href="/apple-touch-icon.png">
<meta name="theme-color" content="#6B4A9E">

<!-- Open Graph (для соцсетей, мессенджеров, Telegram) -->
<meta property="og:type" content="website">
<meta property="og:url" content="https://gruppa-alfa.ru/">
<meta property="og:title" content="Группа Альфа — IT-автоматизация бизнеса: цифровые системы РФ под ключ">
<meta property="og:description" content="Подключаем к ЕГАИС, Честному знаку, Меркурию, ФГИС ЛК, Зерно, ЭДО. Внедряем 1С и кассы. Работаем с 2015 года по всей России.">
<meta property="og:image" content="https://gruppa-alfa.ru/og-cover.jpg">
<meta property="og:image:width" content="1200">
<meta property="og:image:height" content="630">
<meta property="og:locale" content="ru_RU">
<meta property="og:site_name" content="Группа Альфа">

<!-- Twitter Card -->
<meta name="twitter:card" content="summary_large_image">
<meta name="twitter:title" content="Группа Альфа — IT-автоматизация бизнеса: цифровые системы РФ под ключ">
<meta name="twitter:description" content="ЕГАИС, Честный знак, Меркурий, ФГИС ЛК, ЭДО, цифровой рубль. Подключим, настроим, обучим.">
<meta name="twitter:image" content="https://gruppa-alfa.ru/og-cover.jpg">

<!-- Schema.org для Яндекса и Google -->
<script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@graph": [
    {
      "@type": "Organization",
      "@id": "https://gruppa-alfa.ru/#organization",
      "name": "Группа Альфа",
      "legalName": "ООО «Альфа-Касса»",
      "url": "https://gruppa-alfa.ru",
      "logo": "https://gruppa-alfa.ru/favicon.svg",
      "description": "IT-автоматизация бизнеса под ключ по всей России с 2015 года. Подключение к ЕГАИС, Честному знаку, Меркурию, ФГИС ЛК, Зерно, ЭДО, цифровому рублю.",
      "foundingDate": "2018-09-14",
      "telephone": "+7-914-758-20-78",
      "email": "office@gruppa-alfa.ru",
      "taxID": "6506011939",
      "vatID": "6506011939",
      "iso6523Code": "0177:1186501006394",
      "areaServed": "RU",
      "address": {
        "@type": "PostalAddress",
        "streetAddress": "ул. Красных Партизан, д. 24, кв. 15",
        "addressLocality": "Оха",
        "addressRegion": "Сахалинская область",
        "postalCode": "694496",
        "addressCountry": "RU"
      },
      "knowsAbout": [
        "ЕГАИС", "Честный знак", "ГИС МТ", "Меркурий", "ФГИС ЛК",
        "ФГИС Зерно", "ЭДО", "Цифровой рубль", "МЧД", "СБП",
        "1С", "АТОЛ", "Frontol", "Маркировка товаров", "54-ФЗ"
      ],
      "sameAs": [
        "https://t.me/gruppaalfa_bot"
      ]
    },
    {
      "@type": "FAQPage",
      "mainEntity": [
        {
          "@type": "Question",
          "name": "Работаете ли вы за пределами Сахалина?",
          "acceptedAnswer": {
            "@type": "Answer",
            "text": "Да. Все цифровые системы (ЕГАИС, Честный знак, Меркурий, ФГИС, ЭДО) подключаются и сопровождаются удалённо — это работает по всей России. Установку оборудования и физический выезд инженера организуем через сеть партнёров от Калининграда до Владивостока."
          }
        },
        {
          "@type": "Question",
          "name": "Сколько времени занимает подключение к ЕГАИС или Честному знаку?",
          "acceptedAnswer": {
            "@type": "Answer",
            "text": "Подключение под ключ к одной системе — от 3 до 7 рабочих дней при наличии всех документов. В пакет входит: получение УКЭП, регистрация в системе, настройка УТМ или ТСД, интеграция с 1С, обучение сотрудников."
          }
        },
        {
          "@type": "Question",
          "name": "Что входит в стоимость автоматизации?",
          "acceptedAnswer": {
            "@type": "Answer",
            "text": "Все пакеты включают подбор и поставку оборудования, настройку под бизнес, регистрацию в ФНС и подключение к ОФД, интеграцию с 1С, обучение персонала и 30 дней бесплатной технической поддержки после запуска. Лицензии 1С и платных систем — отдельно."
          }
        },
        {
          "@type": "Question",
          "name": "Что меняется в ЕГАИС с 1 апреля 2026 года?",
          "acceptedAnswer": {
            "@type": "Answer",
            "text": "Вводится поэкземплярный учёт пива и пивных напитков — каждая бутылка должна быть отсканирована и списана отдельно. Нужно: настроенный 2D-сканер на кассе, ТСД для приёмки, обновлённое кассовое ПО."
          }
        },
        {
          "@type": "Question",
          "name": "Когда нужно готовить кассу к цифровому рублю?",
          "acceptedAnswer": {
            "@type": "Answer",
            "text": "С 1 сентября 2026 года крупнейшие торговые сети обязаны принимать цифровой рубль на кассе. Для подготовки нужны: совместимая ККТ, обновлённая прошивка, доработка кассового ПО, обучение кассиров."
          }
        },
        {
          "@type": "Question",
          "name": "Можете ли вы интегрировать новую кассу с моей старой 1С?",
          "acceptedAnswer": {
            "@type": "Answer",
            "text": "Да. Интегрируем кассы АТОЛ, Эвотор, Штрих-М, Artix, Frontol с любой конфигурацией 1С: Бухгалтерия, Розница, Управление торговлей, УНФ, ERP, отраслевые. Если конфигурация типовая — настройка стандартная. Если самописная — пишем коннектор отдельно."
          }
        },
        {
          "@type": "Question",
          "name": "Кто заплатит за лицензии и подписки?",
          "acceptedAnswer": {
            "@type": "Answer",
            "text": "Лицензии 1С, оператора ЭДО, ОФД, обновления Frontol — оплачивает клиент напрямую вендорам по официальным ценам. Наши работы по внедрению, настройке и сопровождению — отдельно по фиксированному прайсу."
          }
        },
        {
          "@type": "Question",
          "name": "Что будет, если касса сломается или система упадёт ночью?",
          "acceptedAnswer": {
            "@type": "Answer",
            "text": "У нас есть тарифы на круглосуточное сопровождение для критичных бизнесов. Реакция — до 1 часа в рабочее время и до 4 часов ночью. Резервная касса в подменный фонд — по подписке."
          }
        }
      ]
    }
  ]
}
</script>

<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Oswald:wght@400;500;600;700&family=Barlow+Condensed:wght@300;400;500;600;700&display=swap" rel="stylesheet">
<style>
:root {
    /* Фирменная фиолетовая гамма из брендбука */
    --purple-deep: #3d2963;
    --purple-brand: #6b4a9e;
    --purple-mid: #7a5bb0;
    --purple-light: #a890d4;
    --purple-pale: #e8ddf5;

    --bg: #1a1030;
    --bg-2: #241744;
    --surface: #2d1e54;

    --ink: #ffffff;
    --ink-dim: rgba(255,255,255,0.72);
    --ink-muted: rgba(255,255,255,0.45);

    --accent: #ff7a1a;
    --accent-soft: rgba(255,122,26,0.15);

    --line: rgba(255,255,255,0.1);
    --line-strong: rgba(255,255,255,0.2);

    --display: 'Oswald', 'Arial Narrow', sans-serif;
    --body: 'Barlow Condensed', 'Arial Narrow', sans-serif;
}

* { margin: 0; padding: 0; box-sizing: border-box; }
html { scroll-behavior: smooth; }

body {
    background: var(--bg);
    color: var(--ink);
    font-family: var(--body);
    font-size: 17px;
    line-height: 1.55;
    overflow-x: hidden;
    -webkit-font-smoothing: antialiased;
}

/* Фирменная сетка точек (как на брендбуке) */
.dot-network {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    pointer-events: none;
    opacity: 0.55;
}

.wrap {
    max-width: 1320px;
    margin: 0 auto;
    padding: 0 32px;
    position: relative;
    z-index: 2;
}

/* === NAVBAR === */
.nav {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    z-index: 100;
    backdrop-filter: blur(16px);
    -webkit-backdrop-filter: blur(16px);
    background: rgba(26,16,48,0.78);
    border-bottom: 1px solid var(--line);
}
.nav-inner {
    max-width: 1320px;
    margin: 0 auto;
    padding: 16px 32px;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 24px;
}
.logo {
    display: flex;
    align-items: center;
    gap: 12px;
    text-decoration: none;
    color: var(--ink);
}
.logo-mark {
    width: 40px;
    height: 40px;
    flex-shrink: 0;
}
.logo-text {
    font-family: var(--display);
    font-weight: 700;
    font-size: 22px;
    letter-spacing: 0.02em;
    line-height: 0.9;
    text-transform: uppercase;
}
.logo-text small {
    display: block;
    font-size: 11px;
    font-weight: 500;
    letter-spacing: 0.25em;
    color: var(--purple-light);
    margin-top: 3px;
}
.nav-links {
    display: flex;
    gap: 32px;
    font-size: 15px;
    font-weight: 500;
    text-transform: uppercase;
    letter-spacing: 0.08em;
}
.nav-links a {
    color: var(--ink-dim);
    text-decoration: none;
    transition: color .2s;
}
.nav-links a:hover { color: var(--ink); }
.nav-phone {
    font-family: var(--display);
    font-weight: 600;
    font-size: 15px;
    color: var(--ink);
    text-decoration: none;
    padding: 10px 18px;
    background: var(--accent);
    border-radius: 999px;
    letter-spacing: 0.04em;
    transition: all .2s;
    white-space: nowrap;
    box-shadow: 0 6px 24px rgba(255,122,26,0.35);
    display: inline-flex;
    align-items: center;
    gap: 8px;
    text-transform: uppercase;
}
.tg-icon {
    width: 18px;
    height: 18px;
    flex-shrink: 0;
}
.nav-phone:hover {
    transform: translateY(-1px);
    box-shadow: 0 10px 30px rgba(255,122,26,0.5);
}

@media (max-width: 900px) {
    .nav-links { display: none; }
    .nav-inner { padding: 14px 20px; }
    .logo-text { font-size: 18px; }
    .logo-text small { font-size: 10px; }
    .nav-phone {
        font-size: 0;
        padding: 10px 12px;
        gap: 0;
    }
    .nav-phone .tg-icon {
        width: 22px;
        height: 22px;
    }
}

/* === HERO === */
.hero {
    padding: 150px 0 110px;
    position: relative;
    overflow: hidden;
    background:
        radial-gradient(ellipse 80% 60% at 80% 30%, rgba(122,91,176,0.45), transparent 60%),
        radial-gradient(ellipse 60% 50% at 10% 80%, rgba(61,41,99,0.6), transparent 60%),
        linear-gradient(180deg, var(--bg) 0%, var(--bg-2) 100%);
}
.hero-content {
    position: relative;
    z-index: 3;
}
.eyebrow {
    display: inline-flex;
    align-items: center;
    gap: 10px;
    padding: 8px 18px;
    background: rgba(255,255,255,0.08);
    border: 1px solid var(--line-strong);
    border-radius: 999px;
    font-family: var(--display);
    font-size: 13px;
    font-weight: 500;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    margin-bottom: 32px;
}
.eyebrow::before {
    content: '';
    width: 8px;
    height: 8px;
    background: var(--accent);
    border-radius: 50%;
    box-shadow: 0 0 12px var(--accent);
    animation: pulse 2s infinite;
}
@keyframes pulse {
    0%, 100% { opacity: 1; transform: scale(1); }
    50% { opacity: 0.5; transform: scale(1.2); }
}

h1.hero-title {
    font-family: var(--display);
    font-weight: 600;
    font-size: clamp(44px, 8vw, 110px);
    line-height: 0.95;
    letter-spacing: -0.01em;
    text-transform: uppercase;
    margin-bottom: 36px;
    max-width: 16ch;
}
h1.hero-title .accent {
    color: var(--accent);
    font-weight: 700;
}
h1.hero-title .outlined {
    -webkit-text-stroke: 2px var(--ink);
    color: transparent;
}

.hero-sub {
    font-size: clamp(17px, 1.4vw, 21px);
    color: var(--ink-dim);
    max-width: 640px;
    margin-bottom: 48px;
    line-height: 1.5;
}

.hero-cta-row {
    display: flex;
    gap: 18px;
    align-items: center;
    flex-wrap: wrap;
    margin-bottom: 72px;
}
.btn {
    display: inline-flex;
    align-items: center;
    gap: 12px;
    padding: 18px 32px;
    font-family: var(--display);
    font-weight: 600;
    font-size: 16px;
    letter-spacing: 0.06em;
    text-transform: uppercase;
    text-decoration: none;
    border-radius: 999px;
    transition: all .25s;
    cursor: pointer;
    border: none;
    white-space: nowrap;
}
.btn-primary {
    background: var(--accent);
    color: var(--ink);
    box-shadow: 0 10px 36px rgba(255,122,26,0.4);
}
.btn-primary:hover {
    transform: translateY(-2px);
    box-shadow: 0 14px 46px rgba(255,122,26,0.55);
}
.btn-ghost {
    background: transparent;
    color: var(--ink);
    border: 1px solid var(--line-strong);
}
.btn-ghost:hover {
    background: rgba(255,255,255,0.08);
    border-color: rgba(255,255,255,0.4);
}
.btn-arrow {
    display: inline-block;
    transition: transform .25s;
}
.btn:hover .btn-arrow { transform: translateX(4px); }

/* Подпись о согласии на обработку ПДн под CTA-кнопкой */
.consent-note {
    font-size: 13px;
    color: var(--ink-muted);
    margin-top: 12px;
    line-height: 1.4;
}
.consent-note a {
    color: var(--ink-dim);
    text-decoration: underline;
    text-decoration-style: dotted;
    text-underline-offset: 3px;
}
.consent-note a:hover { color: var(--accent); }

/* Полоса брендов */
.brands {
    display: flex;
    align-items: center;
    gap: 44px;
    flex-wrap: wrap;
    padding: 28px 0;
    border-top: 1px solid var(--line);
    border-bottom: 1px solid var(--line);
}
.brands-label {
    font-family: var(--display);
    font-size: 12px;
    font-weight: 500;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    color: var(--ink-muted);
}
.brand-item {
    font-family: var(--display);
    font-weight: 700;
    font-size: 24px;
    color: var(--ink-dim);
    letter-spacing: 0.02em;
    text-transform: uppercase;
    transition: color .2s;
}
.brand-item:hover { color: var(--ink); }

/* === METRICS === */
.metrics {
    padding: 90px 0;
    border-bottom: 1px solid var(--line);
    position: relative;
    overflow: hidden;
}
.metrics-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 0;
}
.metric {
    padding: 24px 32px;
    border-left: 1px solid var(--line);
}
.metric:first-child { border-left: none; padding-left: 0; }
.metric-num {
    font-family: var(--display);
    font-weight: 600;
    font-size: clamp(52px, 6.5vw, 84px);
    line-height: 1;
    letter-spacing: -0.02em;
    color: var(--ink);
    margin-bottom: 14px;
}
.metric-num .unit {
    color: var(--accent);
    font-size: 0.55em;
    font-weight: 500;
    margin-left: 4px;
}
.metric-label {
    font-size: 15px;
    color: var(--ink-dim);
    line-height: 1.4;
    max-width: 24ch;
    text-transform: uppercase;
    letter-spacing: 0.04em;
    font-weight: 500;
}
@media (max-width: 800px) {
    .metrics-grid { grid-template-columns: repeat(2, 1fr); }
    .metric:nth-child(3) { border-left: none; padding-left: 0; }
    .metric:nth-child(1), .metric:nth-child(2) { border-bottom: 1px solid var(--line); padding-bottom: 40px; }
    .metric:nth-child(3), .metric:nth-child(4) { padding-top: 40px; }
}

/* === SERVICES === */
.services {
    padding: 120px 0;
    position: relative;
}

/* Блок "Цифровые системы РФ" — раскладка 3 featured + 5 compact */
.systems {
    padding: 100px 0 60px;
    position: relative;
    background: linear-gradient(180deg, transparent 0%, rgba(107,74,158,0.08) 50%, transparent 100%);
}

/* Featured: 3 крупные карточки в ряд */
.systems-featured {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 24px;
    margin-top: 48px;
    margin-bottom: 24px;
}
.system-card-featured {
    background: rgba(255,255,255,0.04);
    border: 1px solid rgba(255,255,255,0.1);
    border-radius: 18px;
    padding: 28px;
    transition: all .25s ease;
    text-decoration: none;
    color: var(--ink);
    display: flex;
    flex-direction: column;
    gap: 14px;
    position: relative;
}
.system-card-featured:hover {
    background: rgba(255,255,255,0.06);
    border-color: rgba(255,122,26,0.4);
    transform: translateY(-3px);
}
.system-card-featured .system-icon-wrap {
    width: 48px;
    height: 48px;
    border-radius: 12px;
    background: rgba(255,122,26,0.15);
    color: var(--accent);
    display: grid;
    place-items: center;
    font-size: 24px;
}
.system-card-featured h3 {
    font-family: var(--display);
    font-weight: 700;
    font-size: 22px;
    line-height: 1.2;
    color: var(--ink);
    margin: 0;
}
.system-card-featured .system-tagline {
    font-size: 14px;
    color: var(--accent);
    font-family: var(--display);
    font-weight: 500;
    text-transform: uppercase;
    letter-spacing: 0.04em;
    margin: 0;
}
.system-card-featured .system-desc {
    font-size: 15px;
    line-height: 1.55;
    color: var(--ink-dim);
    margin: 0;
}
.system-card-featured .system-link {
    margin-top: auto;
    color: var(--accent);
    font-size: 14px;
    font-family: var(--display);
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.06em;
    padding-top: 12px;
}
.system-card-featured .system-link::after {
    content: ' →';
    transition: transform .25s;
    display: inline-block;
}
.system-card-featured:hover .system-link::after {
    transform: translateX(4px);
}

/* Compact: 5 мелких карточек в ряд */
.systems-compact {
    display: grid;
    grid-template-columns: repeat(5, 1fr);
    gap: 16px;
    list-style: none;
    padding: 0;
    margin: 0;
}
.system-card-compact {
    background: rgba(255,255,255,0.03);
    border: 1px solid rgba(255,255,255,0.08);
    border-radius: 14px;
    padding: 20px 18px;
    transition: all .25s ease;
    text-decoration: none;
    color: var(--ink);
    display: flex;
    flex-direction: column;
    gap: 8px;
    height: 100%;
}
.system-card-compact:hover {
    background: rgba(255,255,255,0.06);
    border-color: rgba(255,122,26,0.35);
    transform: translateY(-2px);
}
.system-card-compact .system-icon-wrap {
    width: 36px;
    height: 36px;
    border-radius: 10px;
    background: rgba(255,122,26,0.12);
    color: var(--accent);
    display: grid;
    place-items: center;
    font-size: 18px;
    margin-bottom: 4px;
}
.system-card-compact h3 {
    font-family: var(--display);
    font-weight: 600;
    font-size: 16px;
    line-height: 1.2;
    color: var(--ink);
    margin: 0;
}
.system-card-compact .system-tagline {
    font-size: 13px;
    line-height: 1.4;
    color: var(--ink-dim);
    margin: 0;
}

@media (max-width: 1023px) {
    .systems-featured { grid-template-columns: repeat(2, 1fr); }
    .systems-compact { grid-template-columns: repeat(2, 1fr); }
}
@media (max-width: 600px) {
    .systems-featured { grid-template-columns: 1fr; gap: 16px; }
    .systems-compact { grid-template-columns: 1fr; gap: 12px; }
    .system-card-featured { padding: 22px; }
}

/* FAQ блок */
.faq {
    padding: 100px 0 120px;
    background: var(--bg-2);
}
.faq-list {
    max-width: 920px;
    margin: 48px auto 0;
    display: flex;
    flex-direction: column;
    gap: 12px;
    list-style: none;
    padding: 0;
}
.faq-item {
    background: rgba(255,255,255,0.03);
    border: 1px solid rgba(255,255,255,0.1);
    border-radius: 14px;
    overflow: hidden;
    transition: all .25s ease;
    padding: 0;
}
.faq-item[open] {
    background: rgba(255,255,255,0.05);
    border-color: rgba(255,122,26,0.4);
}
.faq-q {
    font-family: var(--display);
    font-weight: 500;
    font-size: 17px;
    color: var(--ink);
    cursor: pointer;
    list-style: none;
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 16px;
    padding: 20px 24px;
    line-height: 1.4;
}
.faq-q::-webkit-details-marker { display: none; }
.faq-q::after {
    content: '';
    width: 18px;
    height: 18px;
    flex-shrink: 0;
    background-image: url("data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 24 24' fill='none' stroke='%23ff7a1a' stroke-width='2.5' stroke-linecap='round' stroke-linejoin='round'><polyline points='6 9 12 15 18 9'/></svg>");
    background-repeat: no-repeat;
    background-size: contain;
    transition: transform .25s ease;
}
.faq-item[open] .faq-q::after {
    transform: rotate(180deg);
}
.faq-a {
    font-size: 15px;
    line-height: 1.65;
    color: var(--ink-dim);
    padding: 0 24px 22px;
    margin: 0;
}
@media (prefers-reduced-motion: reduce) {
    .faq-item, .faq-q::after { transition: none; }
}

@media (max-width: 768px) {
    .faq-q { font-size: 15px; padding: 18px 20px; }
    .faq-a { font-size: 14px; padding: 0 20px 18px; }
}
@media (min-width: 769px) and (max-width: 1100px) {
    .systems-grid { grid-template-columns: repeat(2, 1fr); }
}
.section-head {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 60px;
    margin-bottom: 72px;
    align-items: end;
}
.section-kicker {
    font-family: var(--display);
    font-weight: 500;
    font-size: 13px;
    letter-spacing: 0.25em;
    text-transform: uppercase;
    color: var(--accent);
    margin-bottom: 20px;
}
.section-kicker::before {
    content: '◆ ';
    color: var(--accent);
}
.section-title {
    font-family: var(--display);
    font-weight: 600;
    font-size: clamp(36px, 5vw, 64px);
    line-height: 1;
    letter-spacing: -0.01em;
    text-transform: uppercase;
}
.section-title .accent { color: var(--accent); font-weight: 700; }
.section-lead {
    font-size: 19px;
    color: var(--ink-dim);
    line-height: 1.55;
}
@media (max-width: 800px) {
    .section-head { grid-template-columns: 1fr; gap: 24px; }
}

.vendors-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 20px;
    margin-bottom: 60px;
}
.vendor {
    background: var(--surface);
    border: 1px solid var(--line);
    border-radius: 20px;
    padding: 28px 24px;
    position: relative;
    transition: all .3s;
    overflow: hidden;
}
.vendor::before {
    content: '';
    position: absolute;
    inset: 0;
    background: radial-gradient(circle at top right, rgba(255,122,26,0.08), transparent 60%);
    opacity: 0;
    transition: opacity .3s;
    pointer-events: none;
}
.vendor:hover {
    transform: translateY(-4px);
    border-color: var(--line-strong);
    box-shadow: 0 20px 50px rgba(0,0,0,0.3);
}
.vendor:hover::before { opacity: 1; }
.vendor:hover .vendor-logo { transform: scale(1.05) rotate(-2deg); }

.vendor-badge {
    display: inline-block;
    padding: 5px 12px;
    background: rgba(255,255,255,0.06);
    border: 1px solid var(--line-strong);
    border-radius: 999px;
    font-family: var(--display);
    font-size: 10px;
    font-weight: 500;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    color: var(--purple-light);
    margin-bottom: 20px;
}
.vendor-logo {
    width: 72px;
    height: 72px;
    margin-bottom: 20px;
    transition: transform .3s;
    border-radius: 14px;
    overflow: hidden;
    box-shadow: 0 8px 20px rgba(0,0,0,0.25);
    background: var(--v-bg, #6B4A9E);
    display: grid;
    place-items: center;
    position: relative;
    padding: 6px;
    box-sizing: border-box;
}
.vendor-logo.has-border {
    border: 2px solid #ececec;
}
.vendor-mono {
    font-family: var(--display);
    font-weight: 800;
    color: var(--v-fg, #ffffff);
    letter-spacing: -0.01em;
    line-height: 1.0;
    text-align: center;
    text-transform: uppercase;
    font-size: var(--v-size, 16px);
    padding: 0 2px;
    word-break: keep-all;
    overflow-wrap: break-word;
}
.vendor-mono small {
    display: block;
    font-size: 0.7em;
    font-weight: 700;
    margin-top: 2px;
    opacity: 0.95;
}
.vendor-title {
    font-family: var(--display);
    font-weight: 600;
    font-size: 22px;
    letter-spacing: 0.02em;
    margin-bottom: 10px;
    text-transform: uppercase;
    line-height: 1.1;
}
.vendor-text {
    font-size: 15px;
    color: var(--ink-dim);
    line-height: 1.5;
}

/* Доп. услуги (горизонтальная полоса под вендорами) */
.services-extra {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 0;
    border-top: 1px solid var(--line);
    border-bottom: 1px solid var(--line);
    margin-top: 20px;
}
.service-extra {
    display: flex;
    align-items: flex-start;
    gap: 14px;
    padding: 24px 20px;
    border-left: 1px solid var(--line);
    transition: background .3s;
}
.service-extra:first-child { border-left: none; }
.service-extra:nth-child(5) {
    border-left: none;
    border-top: 1px solid var(--line);
}
.service-extra:nth-child(n+5) {
    border-top: 1px solid var(--line);
}
.service-extra:hover { background: rgba(107,74,158,0.1); }
.service-extra:hover .service-extra-icon {
    background: var(--accent);
    border-color: var(--accent);
}
.service-extra-icon {
    width: 44px;
    height: 44px;
    background: rgba(255,122,26,0.1);
    border: 1px solid rgba(255,122,26,0.3);
    border-radius: 10px;
    display: grid;
    place-items: center;
    flex-shrink: 0;
    transition: all .3s;
    color: var(--ink);
}
.service-extra-icon svg { width: 20px; height: 20px; }
.service-extra-title {
    font-family: var(--display);
    font-weight: 600;
    font-size: 16px;
    letter-spacing: 0.02em;
    margin-bottom: 4px;
    text-transform: uppercase;
    line-height: 1.2;
}
.service-extra-text {
    font-size: 13px;
    color: var(--ink-dim);
    line-height: 1.4;
}

@media (max-width: 1100px) {
    .vendors-grid { grid-template-columns: repeat(3, 1fr); }
    .services-extra { grid-template-columns: repeat(2, 1fr); }
    .service-extra { border-left: none !important; border-top: 1px solid var(--line) !important; }
    .service-extra:nth-child(odd) { border-right: 1px solid var(--line); }
    .service-extra:nth-child(1), .service-extra:nth-child(2) { border-top: none !important; }
}
@media (max-width: 800px) {
    .vendors-grid { grid-template-columns: repeat(2, 1fr); }
}
@media (max-width: 500px) {
    .vendors-grid { grid-template-columns: 1fr; }
    .services-extra { grid-template-columns: 1fr; }
    .service-extra { border-left: none !important; border-right: none !important; border-top: 1px solid var(--line) !important; }
    .service-extra:first-child { border-top: none !important; }
}

/* === PROCESS === */
.process {
    padding: 120px 0;
    background: var(--bg-2);
    border-top: 1px solid var(--line);
    border-bottom: 1px solid var(--line);
    position: relative;
    overflow: hidden;
}
.process-steps {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 16px;
    position: relative;
    margin-top: 48px;
    list-style: none;
    padding: 0;
}
.step {
    position: relative;
    background: rgba(255,255,255,0.04);
    border: 1px solid rgba(255,255,255,0.1);
    border-radius: 18px;
    padding: 24px;
    transition: all .25s ease;
}
.step:hover {
    background: rgba(255,255,255,0.06);
    border-color: rgba(255,122,26,0.4);
    transform: translateY(-2px);
}
.step-head {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-bottom: 22px;
}
.step-icon {
    width: 44px;
    height: 44px;
    border-radius: 12px;
    background: rgba(255,122,26,0.15);
    color: var(--accent);
    display: grid;
    place-items: center;
    font-size: 22px;
}
.step-num {
    font-family: var(--display);
    font-weight: 700;
    font-size: 26px;
    color: rgba(255,255,255,0.18);
    letter-spacing: -0.01em;
}
.step-title {
    font-family: var(--display);
    font-weight: 600;
    font-size: 19px;
    line-height: 1.2;
    margin-bottom: 8px;
    color: var(--ink);
    text-transform: none;
    letter-spacing: 0;
}
.step-text {
    font-size: 14px;
    color: var(--ink-dim);
    line-height: 1.55;
}
@media (max-width: 1023px) {
    .process-steps { grid-template-columns: repeat(2, 1fr); gap: 16px; }
}
@media (max-width: 600px) {
    .process-steps { grid-template-columns: 1fr; }
}

/* === CALL CTA === */
.call-cta {
    padding: 130px 0;
    position: relative;
    overflow: hidden;
}
.call-cta-inner {
    background: linear-gradient(135deg, var(--purple-brand) 0%, var(--purple-deep) 100%);
    border-radius: 32px;
    padding: 72px 56px;
    position: relative;
    overflow: hidden;
    display: grid;
    grid-template-columns: 1.1fr 1fr;
    gap: 56px;
    align-items: center;
}
.call-cta-inner::after {
    content: 'АЛЬФА';
    position: absolute;
    bottom: -40px;
    right: -20px;
    font-family: var(--display);
    font-weight: 700;
    font-size: clamp(100px, 18vw, 240px);
    color: rgba(255,255,255,0.06);
    letter-spacing: -0.02em;
    line-height: 0.8;
    pointer-events: none;
    z-index: 0;
    text-transform: uppercase;
}
.call-cta-text { position: relative; z-index: 1; }
.call-cta-title {
    font-family: var(--display);
    font-weight: 600;
    font-size: clamp(32px, 4.5vw, 56px);
    line-height: 1.02;
    letter-spacing: -0.01em;
    margin-bottom: 22px;
    text-transform: uppercase;
}
.call-cta-title .accent { color: var(--accent); font-weight: 700; }
.call-cta-sub {
    font-size: 18px;
    line-height: 1.5;
    max-width: 42ch;
    opacity: 0.88;
}
.call-phone-block {
    position: relative;
    z-index: 1;
    background: rgba(0,0,0,0.2);
    backdrop-filter: blur(10px);
    border: 1px solid rgba(255,255,255,0.15);
    padding: 36px 32px;
    border-radius: 24px;
    text-align: center;
}
.call-phone-label {
    font-family: var(--display);
    font-size: 12px;
    letter-spacing: 0.25em;
    text-transform: uppercase;
    color: rgba(255,255,255,0.7);
    margin-bottom: 16px;
}
.call-phone-num {
    display: block;
    font-family: var(--display);
    font-weight: 600;
    font-size: clamp(28px, 3.2vw, 40px);
    letter-spacing: 0.01em;
    color: var(--ink);
    text-decoration: none;
    margin-bottom: 12px;
    transition: color .2s;
}
.call-phone-num:hover { color: var(--accent); }
.call-phone-hours {
    font-size: 14px;
    color: rgba(255,255,255,0.65);
    margin-bottom: 24px;
}
.call-phone-btn {
    display: block;
    width: 100%;
    padding: 16px;
    background: var(--accent);
    color: var(--ink);
    text-decoration: none;
    font-family: var(--display);
    font-weight: 600;
    font-size: 15px;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    border-radius: 999px;
    transition: all .2s;
    box-shadow: 0 8px 24px rgba(255,122,26,0.35);
}
.call-phone-btn:hover {
    transform: translateY(-2px);
    box-shadow: 0 12px 32px rgba(255,122,26,0.5);
}

/* Telegram специфика */
.tg-badge-top {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    padding: 6px 14px;
    background: rgba(0,136,204,0.2);
    border: 1px solid rgba(64,179,224,0.5);
    border-radius: 999px;
    font-family: var(--display);
    font-size: 12px;
    font-weight: 500;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: #b3e0f5;
    margin-bottom: 22px;
}
.tg-badge-top svg {
    width: 16px;
    height: 16px;
    color: #40b3e0;
}
.tg-features {
    list-style: none;
    margin-top: 28px;
    display: flex;
    flex-direction: column;
    gap: 12px;
}
.tg-features li {
    display: flex;
    align-items: center;
    gap: 12px;
    font-size: 15px;
    color: rgba(255,255,255,0.92);
}
.tg-features li svg {
    width: 18px;
    height: 18px;
    color: var(--accent);
    flex-shrink: 0;
}
.tg-block {
    text-align: center;
}
.tg-mascot {
    width: 84px;
    height: 84px;
    margin: 0 auto 18px;
    filter: drop-shadow(0 10px 25px rgba(0,136,204,0.5));
}
.tg-mascot svg { width: 100%; height: 100%; }
.tg-handle {
    font-family: var(--display);
    font-weight: 600;
    font-size: 22px;
    color: var(--ink);
    margin-bottom: 22px;
    letter-spacing: 0.02em;
}
.tg-btn {
    background: #0088cc !important;
    box-shadow: 0 8px 24px rgba(0,136,204,0.45) !important;
}
.tg-btn:hover {
    background: #0099e6 !important;
    box-shadow: 0 12px 32px rgba(0,136,204,0.6) !important;
}
.tg-alt {
    margin-top: 16px;
    font-size: 13px;
    color: rgba(255,255,255,0.55);
}
.tg-alt a {
    color: var(--purple-light);
    text-decoration: none;
    border-bottom: 1px dashed var(--purple-light);
}

@media (max-width: 900px) {
    .call-cta-inner {
        grid-template-columns: 1fr;
        padding: 48px 32px;
        gap: 36px;
    }
}

/* === CONTACT FORM === */
.contact {
    padding: 120px 0;
    position: relative;
}
.contact-grid {
    display: grid;
    grid-template-columns: 1fr 1.1fr;
    gap: 64px;
    align-items: start;
}
.contact-info-title {
    font-family: var(--display);
    font-weight: 600;
    font-size: clamp(36px, 4.5vw, 56px);
    line-height: 1;
    letter-spacing: -0.01em;
    margin-bottom: 28px;
    text-transform: uppercase;
}
.contact-info-title .accent { color: var(--accent); font-weight: 700; }
.contact-info-sub {
    font-size: 18px;
    color: var(--ink-dim);
    line-height: 1.55;
    margin-bottom: 40px;
}
.contact-items {
    display: flex;
    flex-direction: column;
    gap: 22px;
}
.contact-item {
    display: flex;
    gap: 18px;
    align-items: flex-start;
    padding: 20px 0;
    border-bottom: 1px solid var(--line);
}
.contact-item:last-child { border-bottom: none; }
.contact-item-icon {
    width: 44px;
    height: 44px;
    background: rgba(255,122,26,0.12);
    border: 1px solid rgba(255,122,26,0.3);
    border-radius: 12px;
    display: grid;
    place-items: center;
    flex-shrink: 0;
}
.contact-item-icon svg { width: 20px; height: 20px; stroke: var(--accent); }
.contact-item-body {
    flex: 1;
    min-width: 0;
}
.contact-item-label {
    font-family: var(--display);
    font-size: 12px;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    color: var(--ink-muted);
    margin-bottom: 4px;
}
.contact-item-value {
    font-size: 18px;
    color: var(--ink);
    text-decoration: none;
    font-weight: 500;
    word-break: break-word;
}
a.contact-item-value:hover { color: var(--accent); }

.tg-card {
    background: linear-gradient(155deg, #0088cc 0%, #005a87 100%);
    border-radius: 24px;
    padding: 40px 36px;
    position: relative;
    overflow: hidden;
    color: var(--ink);
}
.tg-card::before {
    content: '';
    position: absolute;
    top: -60px;
    right: -60px;
    width: 220px;
    height: 220px;
    background: radial-gradient(circle, rgba(255,255,255,0.15), transparent 70%);
    filter: blur(10px);
}
.tg-card::after {
    content: '';
    position: absolute;
    bottom: -80px;
    left: -80px;
    width: 240px;
    height: 240px;
    background: radial-gradient(circle, rgba(255,122,26,0.2), transparent 70%);
    filter: blur(20px);
}
.tg-card-head {
    display: flex;
    align-items: center;
    gap: 18px;
    margin-bottom: 28px;
    position: relative;
}
.tg-card-icon {
    width: 56px;
    height: 56px;
    flex-shrink: 0;
    filter: drop-shadow(0 6px 16px rgba(0,0,0,0.3));
}
.tg-card-title {
    font-family: var(--display);
    font-weight: 600;
    font-size: 24px;
    letter-spacing: 0.02em;
    text-transform: uppercase;
    line-height: 1.1;
    margin-bottom: 4px;
}
.tg-card-sub {
    font-size: 14px;
    opacity: 0.85;
}
.tg-card-steps {
    list-style: none;
    padding: 0;
    margin: 0 0 28px 0;
    display: flex;
    flex-direction: column;
    gap: 14px;
    position: relative;
}
.tg-card-steps li {
    display: flex;
    align-items: center;
    gap: 14px;
    font-size: 16px;
    line-height: 1.4;
    padding: 14px 18px;
    background: rgba(0,0,0,0.2);
    border-radius: 12px;
    border: 1px solid rgba(255,255,255,0.1);
}
.tg-card-steps li span {
    flex-shrink: 0;
    width: 28px;
    height: 28px;
    background: var(--accent);
    border-radius: 50%;
    display: grid;
    place-items: center;
    font-family: var(--display);
    font-weight: 700;
    font-size: 14px;
}
.tg-card-steps li b {
    font-weight: 600;
    color: #b3e0f5;
}
.tg-card-btn {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 10px;
    width: 100%;
    padding: 18px;
    background: var(--accent);
    color: var(--ink);
    text-decoration: none;
    font-family: var(--display);
    font-weight: 600;
    font-size: 17px;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    border-radius: 999px;
    transition: all .2s;
    box-shadow: 0 10px 30px rgba(255,122,26,0.45);
    position: relative;
}
.tg-card-btn svg {
    width: 22px;
    height: 22px;
}
.tg-card-btn:hover {
    transform: translateY(-2px);
    box-shadow: 0 14px 40px rgba(255,122,26,0.6);
}
.tg-card-note {
    font-size: 13px;
    color: rgba(255,255,255,0.75);
    text-align: center;
    margin-top: 18px;
    line-height: 1.5;
    position: relative;
}
.tg-card-note a {
    color: #ffffff;
    text-decoration: underline;
    text-underline-offset: 2px;
}

@media (max-width: 900px) {
    .contact-grid { grid-template-columns: 1fr; gap: 48px; }
    .tg-card { padding: 32px 24px; }
}

/* === ФОРМА ЗАЯВКИ === */
.lead-form {
    background: rgba(255,255,255,0.03);
    border: 1px solid rgba(255,255,255,0.1);
    border-radius: 18px;
    padding: 32px;
}
.lead-form h3 {
    font-family: var(--display);
    font-weight: 600;
    font-size: 22px;
    color: var(--ink);
    margin: 0 0 6px;
    text-transform: none;
    letter-spacing: 0;
}
.lead-form-sub {
    font-size: 14px;
    color: var(--ink-muted);
    margin: 0 0 24px;
}
.form-field {
    margin-bottom: 18px;
}
.form-field label {
    display: block;
    font-family: var(--display);
    font-weight: 500;
    font-size: 13px;
    color: var(--ink-dim);
    margin-bottom: 8px;
    letter-spacing: 0.02em;
}
.form-field label .req {
    color: var(--accent);
    margin-left: 2px;
}
.form-field input,
.form-field select,
.form-field textarea {
    width: 100%;
    background: rgba(255,255,255,0.05);
    border: 1px solid rgba(255,255,255,0.15);
    color: var(--ink);
    font-family: var(--body);
    font-size: 16px;
    padding: 12px 14px;
    border-radius: 10px;
    transition: border-color .2s, background .2s;
    box-sizing: border-box;
}
.form-field input::placeholder,
.form-field textarea::placeholder {
    color: rgba(255,255,255,0.35);
}
.form-field input:focus,
.form-field select:focus,
.form-field textarea:focus {
    outline: none;
    border-color: var(--accent);
    background: rgba(255,255,255,0.08);
}
.form-field select {
    cursor: pointer;
    appearance: none;
    -webkit-appearance: none;
    background-image: url("data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 24 24' fill='none' stroke='%23ff7a1a' stroke-width='2' stroke-linecap='round'><polyline points='6 9 12 15 18 9'/></svg>");
    background-repeat: no-repeat;
    background-position: right 14px center;
    background-size: 16px;
    padding-right: 40px;
}
.form-field select option {
    background: var(--bg-2);
    color: var(--ink);
}
.form-field textarea {
    min-height: 100px;
    resize: vertical;
    font-family: var(--body);
}
.form-checkbox {
    display: flex;
    align-items: flex-start;
    gap: 10px;
    margin-bottom: 18px;
    cursor: pointer;
}
.form-checkbox input[type="checkbox"] {
    width: 18px;
    height: 18px;
    margin-top: 2px;
    accent-color: var(--accent);
    cursor: pointer;
    flex-shrink: 0;
}
.form-checkbox-text {
    font-size: 13px;
    line-height: 1.5;
    color: var(--ink-dim);
}
.form-checkbox-text a {
    color: var(--ink);
    text-decoration: underline;
    text-underline-offset: 2px;
}
.form-submit {
    width: 100%;
    background: var(--accent);
    color: #fff;
    border: none;
    font-family: var(--display);
    font-weight: 600;
    font-size: 15px;
    padding: 14px 20px;
    border-radius: 10px;
    cursor: pointer;
    transition: background .2s;
    text-transform: uppercase;
    letter-spacing: 0.04em;
}
.form-submit:hover { background: #e96b15; }
.form-submit:disabled {
    opacity: 0.6;
    cursor: not-allowed;
}
.form-status {
    margin-top: 14px;
    padding: 12px 14px;
    border-radius: 10px;
    font-size: 14px;
    line-height: 1.45;
    display: none;
}
.form-status.is-success {
    display: block;
    background: rgba(34,197,94,0.12);
    border: 1px solid rgba(34,197,94,0.4);
    color: #86efac;
}
.form-status.is-error {
    display: block;
    background: rgba(239,68,68,0.12);
    border: 1px solid rgba(239,68,68,0.4);
    color: #fca5a5;
}

@media (max-width: 600px) {
    .lead-form { padding: 24px 20px; }
}

/* === FOOTER === */
.footer {
    padding: 48px 0 32px;
    border-top: 1px solid var(--line);
    background: var(--bg-2);
}
.footer-inner {
    display: flex;
    justify-content: space-between;
    align-items: center;
    flex-wrap: wrap;
    gap: 20px;
}
.footer-copy {
    font-size: 14px;
    color: var(--ink-muted);
}
.footer-links {
    display: flex;
    gap: 24px;
    font-size: 14px;
}
.footer-links a {
    color: var(--ink-dim);
    text-decoration: none;
}
.footer-links a:hover { color: var(--ink); }

/* Анимация появления */
@keyframes slideUp {
    from { opacity: 0; transform: translateY(30px); }
    to { opacity: 1; transform: translateY(0); }
}
.reveal {
    opacity: 0;
    animation: slideUp 0.8s ease forwards;
}
.reveal-1 { animation-delay: 0.1s; }
.reveal-2 { animation-delay: 0.25s; }
.reveal-3 { animation-delay: 0.4s; }
.reveal-4 { animation-delay: 0.55s; }
.reveal-5 { animation-delay: 0.7s; }

</style>
<link rel="stylesheet" href="assets/css/hero.css">
